FROM node:18-alpine

WORKDIR /usr/src/app

COPY package*.json ./
RUN npm install --production

# Copy everything for full deployment
COPY . .

# Generate native certificates inside the container safely
RUN node scripts/generateCerts.js

EXPOSE 4433

CMD ["node", "gateway/server.js"]
